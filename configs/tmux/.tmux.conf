setw -g mouse on

# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'fcsonline/tmux-thumbs'

setw -g mode-keys vi

unbind-key C-b
set -g prefix C-a
bind-key C-a send-prefix

unbind [
bind Escape copy-mode
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip -i -selection clipboard"
bind P paste-buffer
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -i -selection clipboard"

bind r source-file ~/.tmux.conf \; display "Reloaded!"
bind | split-window -h
bind - split-window -v

# Navegação com smart-splits.nvim
bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iq nvim && tmux send-keys C-h) || tmux select-pane -L"
bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iq nvim && tmux send-keys C-j) || tmux select-pane -D"
bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iq nvim && tmux send-keys C-k) || tmux select-pane -U"
bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iq nvim && tmux send-keys C-l) || tmux select-pane -R"

bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5
bind N switch-client -l

set -g default-terminal "screen-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

set -g @continuum-restore 'on'
set -g status-interval 5
set -s escape-time 0
set -g base-index 1
set -g status-position top
setw -g pane-base-index 1
setw -g monitor-activity on
set -g visual-activity on
setw -g mouse on
set-option -g focus-events on
bind-key x kill-pane # skip "kill-pane 1? (y/n)" prompt
set -g detach-on-destroy off  # don't exit from tmux when closing a session
set -g renumber-windows on   # renumber all windows when any window is closed
set -g history-limit 1000000 # increase history size (from 2,000)

bind-key "T" run-shell "sesh connect \"$(\
		sesh list | fzf-tmux -p 55%,60% \
			--no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
			--header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
			--bind 'tab:down,btab:up' \
			--bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list)' \
			--bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t)' \
			--bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c)' \
			--bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z)' \
			--bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
			--bind 'ctrl-d:execute(tmux kill-session -t {})+change-prompt(⚡  )+reload(sesh list)'
)\""

# --- THEME ---
set -g status-bg '#1f2335'
set -g status-fg '#a9b1d6'

# -- Active/Inactive Panes
set -g pane-border-style 'fg=#3b4261'
set -g pane-active-border-style 'fg=#a9b1d6'

# -- Status Line
set -g status-justify 'left'
set -g status-left-length 200
set -g status-right-length 200

# -- Windows
setw -g window-status-separator ''
setw -g window-status-format "#[fg=#a9b1d6,bg=#292e42] #I 󰖲 #W "
setw -g window-status-current-format "#[fg=#1f2335,bg=#bb9af7] #I 󰖲 #W "

# -- Left side
set -g status-left "#[fg=#1f2335,bg=#7aa2f7] 󰒋 #S #[fg=#7aa2f7,bg=#1f2335]"

# -- Right side
set -g status-right "#[fg=#a9b1d6,bg=#292e42] #(git branch --show-current | sed \"s/ (HEAD)//g\") 󰘬 #[fg=#1f2335,bg=#a9b1d6] %H:%M %d-%b-%y "


run '~/.tmux/plugins/tpm/tpm'

# Deixa tmux preservar e atualizar variáveis importantes:
set -g update-environment "DISPLAY XAUTHORITY GPG_AGENT_INFO SSH_AUTH_SOCK GPG_TTY"
